safety:
  fail_closed: true

# Existing structure kept (plus USD breakers added)
account:
  # Existing (not used by circuit breaker yet, kept for future)
  max_drawdown_pct: 10.0
  max_daily_loss_pct: 3.0

  # Used now: circuit breakers based on pnl.json values (USD)
  max_drawdown_usd: 10.0
  max_daily_loss_usd: 0.10

trade:
  max_open_positions: 10
  max_notional_usd_per_trade: 20.0

  # Caps
  max_trades_per_day: 6
  max_trades_per_day_per_pair: 3

  # Choice C: do NOT consume daily trade count in dry-run
  count_trades_in_dry_run: false

leverage_caps:
  stocks: 1.25
  crypto: 1.50

# File-based controls (maps to UI buttons)
controls:
  pause_file: "/run/trading/PAUSE"
  kill_switch_file: "/run/trading/KILL_SWITCH"